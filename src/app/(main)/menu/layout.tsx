import type { ReactNode } from 'react';

export const metadata = {
  title: 'Code Cook Menu',
  description: 'A simple Next.js app',
};

export default function MenuLayout({ children }: { children: ReactNode }) {
  return (
    children
  );
}
